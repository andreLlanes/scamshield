# Where the FastAPI backend lives. `next.config.ts` rewrites /api/* to this
# host, so the browser always talks to the Next.js origin and never hits CORS.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
